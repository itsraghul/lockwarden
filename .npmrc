engine-strict=true
# lock-warden depends on lockwarden via a plain semver range (so `npm publish`
# needs no workspace-protocol rewriting); link the local copy during dev.
link-workspace-packages=true
