// Malicious-pattern fixture: a postinstall script carrying a GitHub token.
// The placeholder below (see the __LW_FIXTURE_*__ convention) is replaced
// with a runtime-built fake token by the integration test before scanning.
const auth = '__LW_FIXTURE_GHP__';
console.log(auth.length);
