packages:
  - packages/*
  - corpus
# Explicitly approved install scripts (pnpm blocks the rest — keep it that way).
allowBuilds:
  '@biomejs/biome': true
  esbuild: true
minimumReleaseAgeExclude:
  - lockwarden@0.1.0
